export type { DaoSpaceId, DaoSpace, DaoResourceLocator, PartitionStrategy } from './types';
export { daoNamespace, DaoNamespaceManager } from './namespace';

export const daoSpaces = {
  name: '@dao/spaces',
  description: '宇 — 空间组织，空间的架构与布局',
}
